import PageBrowse from "../../components/PageBrowse";
import CompLayoutApp from "../../components/CompLayoutApp";

const Stories = () => {
  return (
    <CompLayoutApp pageName="Browse">
      <PageBrowse pageName="Browse" />
    </CompLayoutApp>
  );
};

export default Stories;
